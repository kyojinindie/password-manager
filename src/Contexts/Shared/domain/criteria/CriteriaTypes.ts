export enum FilterOperator {
  EQUAL = '=',
  NOT_EQUAL = '!=',
  GT = '>',
  GTE = '>=',
  LT = '<',
  LTE = '<=',
  CONTAINS = 'CONTAINS',
  NOT_CONTAINS = 'NOT_CONTAINS',
  IN = 'IN',
  NOT_IN = 'NOT_IN',
}

export enum OrderType {
  ASC = 'asc',
  DESC = 'desc',
  NONE = 'none',
}
